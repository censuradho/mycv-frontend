import { styled } from "stitches.config";


export const Container = styled('div', {
  display: 'inline',
  a: {
    color: '$primary',
    textAlign: 'left',
    fontSize: '$xsm',
    fontWeight: 400,
    textDecoration: 'underline'
  }
})
