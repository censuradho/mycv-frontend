import { styled } from 'stitches.config'

export const Container = styled('header', {
  width: '100%',
  height: '3.75rem',
  borderBottom: '1px solid $border',
  display: 'flex',
  alignItems: 'center',
  padding: '0px 1rem'
})